{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "entra-phishing-detection.fullname" . }}-pprof
  labels:
    {{- include "entra-phishing-detection.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotationsPprof }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.classNamePprof }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.ingress.tlsPprof }}
  tls:
    {{- range .Values.ingress.tlsPprof }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hostsPprof }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "entra-phishing-detection.fullname" $ }}
                port:
                  number: {{ $.Values.service.portPprof }}
          {{- end }}
    {{- end }}
{{- end }}
